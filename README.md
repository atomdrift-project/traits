# traits

Development and staging tree for the YAML/YARA rules consumed by
[cleave](https://github.com/atomdrift-project/cleave).

End users should install or update traits through cleave rather than cloning
this staging repository:

```bash
cleave update-rules
cleave version
```
